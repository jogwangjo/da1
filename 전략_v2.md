# 0.8+ 전략 — 실행만 하면 됨

## 평가 산식
```
Score = 0.9 × ADS + 0.1 × CPS
ADS = 0.5×(1-FileEER) + 0.2×(1-VoiceEER) + 0.3×(1-MusicEER)
```

## 0.8 달성 조건
CPS≈0.993이면 **ADS ≥ 0.789** 필요.
→ FileEER≤0.25, VoiceEER≤0.08, MusicEER≤0.12면 충분.

## 현재 vs 목표
| | 현재 | 목표 |
|---|---|---|
| FileEER | 0.684 | **0.25** |
| VoiceEER | 0.176 | **0.08** |
| MusicEER | 0.473 | **0.12** |
| ADS | 0.658 | **0.790** |
| CPS | 0.989 | **0.993** |
| **총점** | 0.691 | **0.819** |

## 왜 0.8이 가능한가

1. **DF-Arena 1B 자체가 14개 벤치마크 평균 EER 8.39%** (zero-shot). 우리가 ASVspoof+Codecfake로 파인튜닝하면 voice/music specialist로 만들 수 있음
2. **RAPTOR 논문**: mHuBERT-Iter2로 100M 모델이 2B 상용 모델을 능가
3. **SONICS**: 49k 곡으로 학습된 음악 fake 탐지 모델이 이미 존재
4. **TTA**: VoIP+노이즈 앙상블로 1~3% EER 추가 감소

## 핵심 파일 4개

| 파일 | 역할 |
|---|---|
| `scripts/build_train_data.py` | ASVspoof+Codecfake+SONICS+LibriSpeech+FMA 자동 구축 |
| `scripts/train_raptor.py` | RAPTOR 스타일 파인튜닝 (mHuBERT + pairwise gated fusion + consistency reg) |
| `submit/script_v2.py` | TTA + 5모델 앙상블 + 하이브리드 Fusion |
| `colab/COLAB_080.md` | Colab 원클릭 실행 가이드 |

## Colab 실행 순서 (6~8시간)

```
셀1: pip install (1분)
셀2: Drive 마운트 (1분)
셀3: build_train_data.py (30분~1시간)
셀4: train_raptor.py (3~4시간) ← 핵심
셀5-6: 결과 확인 + 모델 복사 (1분)
셀7: script_v2.py 추론 (30분~1시간)
셀8: 검증 (10분)
```

## 최대 학습 데이터 구성

| 데이터 | 용도 | 파일 수 |
|---|---|---|
| LibriSpeech test-clean | Voice real | 3,000 |
| ASVspoof 2019 LA train | Voice fake | 5,000 |
| Codecfake (HF) | Voice fake (코덱) | 2,000 |
| MUSDB18 sample | Music real (보컬제거) | 200 |
| FMA small | Music real | 500 |
| SONICS (HF) | Music fake (Suno/Udio) | 3,000 |

## 참고 논문 (10편)

1. Kulkarni "RAPTOR" Interspeech 2026 — mHuBERT pairwise gated fusion
2. Kulkarni "Speech DF Arena" 2025 — 14 벤치마크 리더보드
3. Borodin "AASIST3" 2024 — KAN + Wav2Vec2
4. Viakhirev "Scalable AASIST" 2025 — MHA fusion
5. Rahman "SONICS" ICLR 2025 — 97k 곡 음악 fake
6. Zhang "FSAT" ICLR 2025 — 선택적 강건 훈련
7. Xie "Wavelet Prompt Tuning" AAAI 2026 — All-type ADD
8. Tak "SSL Anti-spoofing" — Wav2Vec2 + AASIST (baseline)
9. "XLSR-Mamba" 2024 — State-space 모델
10. "Nes2Net" 2024 — Nested Squeezed Excitation
