# 대회 분석 및 전략

- 대회: 딥보이스 범죄 대응을 위한 AI 탐지 모델 경진대회 (DACON 236749)
- 일정: 대회 시작 2026-08-26 / 제출 마감 09-29 10:00 / 종료 09-30
- 학습 데이터 미제공 → 직접 구축 필요
- 코드 제출: 오프라인, L4 GPU(22.4GB), 추론 ≤60분(1,200파일), 설치 ≤10분, 용량 ≤10GB(zip)/32GB

## 평가 산식 (확정)

```
Score = 0.9 × ADS + 0.1 × CPS

ADS = 0.5×(1−File EER) + 0.2×(1−Voice EER) + 0.3×(1−Music EER)
CPS = 0.5×VoicePresence AUC + 0.5×MusicPresence AUC

EER: fpr/fnr 교차점 (FAKE=양성). Voice EER은 음성 존재 샘플만, Music EER은 음악 존재 샘플만.
```

### 리더보드 3개 숫자 해석
[총점, ADS, CPS] — Baseline 검증: 0.9×0.65775 + 0.1×0.98932 = 0.69091 ✓

| 팀 | 총점 | ADS | CPS |
|---|---|---|---|
| Devfiance(1위) | 0.70100 | 0.66896 | 0.98932 |
| Baseline | 0.69091 | 0.65775 | 0.98932 |

→ 1위도 presence는 baseline 그대로(CPS 동일). 개선 여지 전부 ADS(Fake 탐지)에 있음.

## 개선 레버 우선순위

1. **Music EER** (ADS 내 가중치 0.3): DF-Arena는 음성 특화라 음악 fake가 가장 약할 것. SONICS(Suno/Udio 가짜곡 49k, HF 공개) + SpecTTTra 사전학습 모델로 보강.
2. **File EER** (가중치 0.5): FILE_FAKE fusion 식(max(VP×VF, MP×MF)) 개선 + 강한 앙상블.
3. **Voice EER** (가중치 0.2): ASVspoof 계열 학습 모델 추가.
4. **CPS**: PANNs Cnn14 이미 0.98932. 상향 여진 +0.005 수준(총점 +0.0005). 후순위, 캘리브레이션만.

## 사용 가능한 자원

### 사전학습 모델 후보 (zero-shot 앙상블용)
- DF-Arena 1B (baseline 보유) — universal, speech 중심
- SSL_Anti-spoofing: wav2vec2-XLS-R + AASIST (Tak et al., github TakHemlata/SSL_Anti-spoofing, pretrained on ASVspoof2019LA)
- XLSR-Mamba / XLSR+SLS / TCM-add / Nes2Net — Speech DF Arena 상위권 mid-size
- SONICS SpecTTTra-alpha/beta/gamma (awsaf49 HF, 17M~24M params, 노래 fake F1 0.78~0.97@120s)

### 학습 데이터 소스 (구축용)
- 음성 real: LibriTTS-R, VCTK, AI Hub 한국어 음성, Common Voice ko
- 음성 fake: ASVspoof 2019/2021 LA+DF, ASVspoof 5 (Zenodo/HF jungjee), In-the-Wild, MLAAD v6(다국어 TTS), Codecfake, LibriSeVoc, SpoofCeleb, WildSpoof
  + 직접 생성: edge-tts/Azure/Google 한국어 TTS, XTTS-v2, F5-TTS, GPT-SoVITS 등
- 노래/음악 real: MUSDB18, FMA, MusicCaps, OpenSinger
- 노래/음악 fake: SONICS fake songs (Suno/Udio, HF awsaf49/sonics), CtrSVDD(Zenodo), SingFake + 직접 생성(MusicGen, Stable Audio, YuE, so-vits-svc/RVC)
- 환경음 fake: EnvSDD
- 혼합/채널: real+fake 믹싱, MP3/AAC/Opus 재인코딩, 전화대역(300–3400Hz) 필터, µ-law, 노이즈

### 제약 관리
- 용량: DF-Arena 4.59GB + HTDemucs + PANNs ≈ 5.3GB → 여유 ~4.7GB(zips 기준 아님 주의, 최종 zip 10GB)
- 시간: HTDemucs 분리가 병목 → 신규 모델은 경량(≤400M) 위주, 세그먼트 배치 처리
- 규칙: 비공개 평가데이터 학습 금지 / 파일 단위 독립 예측 준수 / 2차 평가 출처 명시

## 실행 플랜

- [x] Phase 0: 공식 산식 오프라인 채점기 `scripts/dacon_metric.py` (더미데이터 검증 완료)
- [x] Phase 0: pseudo-eval 빌더 `scripts/build_pseudo_eval.py`
      (LibriSpeech/edge-tts/MMS-TTS-ko/FMA/MUSDB-stems/SONICS/MusicGen + mp3·전화·µlaw·노이즈 FX)
- [x] Phase 1: 모델 주 `zoo/` — df_arena, ssl_aasist(ONNX 로컬검증 완료), xlsr_mamba,
      sonics 3종, panns presence
- [x] Phase 1: 배치 추론 `scripts/run_zoo.py` (극성 자동판정 내장)
- [x] Phase 1: fusion 그리드서치 `scripts/fuse_search.py` (F1~F4 × α×gate, 공식산식 랭킹)
- [x] Colab 가이드 `colab/PHASE1.md` (셀 7개 copy-paste)
- [ ] **다음 액션: Colab에서 PHASE1 셀 순서대로 실행 → fusion_results.csv 확인**
- [ ] Phase 2: 통합 코퍼스 파인튜닝 (XLS-R-300M + AASIST), music specialist
- [ ] Phase 3: 제출용 script.py 재작성 (앙상블+fusion 반영, L4 60분 예산 측정)
- [ ] Phase 4: 실제 리더보드 검증 (제출 1회) → pseudo-eval과 괴리 분석

## 아티팩트 지도

```
dacon_metric.py        공식 산식(EER/AUC) 그대로 구현 — 모든 실험의 판정기
build_pseudo_eval.py   라벨 있는 자체 검증셋 생성(제출횟수 안 쓰고 반복 실험)
zoo/                   모델 래퍼(공통 API: load/score/predict_file)
run_zoo.py             pseudo-eval 전체 추론 → preds/<model>__{voice,music,file}.csv
fuse_search.py         역할별 앙상블 + FILE fusion 식 탐색 → fusion_results.csv
colab/PHASE1.md        Colab 셀 단위 실행 가이드
```
